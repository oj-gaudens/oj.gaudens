<!DOCTYPE html>
<html lang="fr" data-theme="dark">
<head>
  <meta charset="UTF-8" />
  <title>{{ title }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <!-- Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&family=JetBrains+Mono&display=swap" rel="stylesheet">

  <!-- CSS EXTERNE (IMPORTANT) -->
  <link rel="stylesheet" href="{{ '/assets/css/main.css' | url }}">
</head>

<body>

  <nav>
    <a href="{{ '/' | url }}">Accueil</a>
    <a href="{{ '/about/' | url }}">Qui je suis</a>
    <a href="{{ '/projects/' | url }}">Projets</a>
    <a href="{{ '/writeups/example-writeup/' | url }}">Write-ups</a>
    <a href="{{ '/experience/' | url }}">Parcours</a>
    <a href="{{ '/skills/' | url }}">Compétences</a>
    <a href="{{ '/contact/' | url }}">Contact</a>
    <button onclick="toggleTheme()">🌗 Thème</button>
  </nav>

  <div class="terminal" id="terminal"></div>

  <main>
    {{ content | safe }}
  </main>

  <!-- JS EXTERNE -->
  <script src="{{ '/assets/js/theme.js' | url }}"></script>

</body>
</html>
